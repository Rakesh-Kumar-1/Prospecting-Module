# Prospecting Messaging Engine

Async Email + SMS engine for the Prospecting Module.

## Setup

1. Install dependencies:  npm install
2. Copy env file:         cp .env.example .env
3. Fill in your .env with real credentials
4. Run SQL files in MySQL
5. Start server:          npm run dev
6. Start worker:          npm run worker